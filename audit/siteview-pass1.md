# Audit — 사이트뷰(지도뷰) Pass 1

- **날짜**: 2026-06-22
- **원본**: Figma node 5047:26122 (`audit/figma-ref/01-siteview-5047.png`)
- **재생성**: `screens/siteview.html` (tokens.css 기반)
- **방식**: get_design_context 실측 좌표 → 1920×971 캔버스 절대배치, 지도=정적 image280

## 결과: 거의 일치 (구조·컬러·타입·레이아웃 ✓)

| 영역 | 일치 | 비고 |
|---|---|---|
| GNB | ⚠ | 구조·nav·CTA·알림뱃지 ✓ / 로고 글리프는 근사(에셋 미추출) |
| 좌측 상세패널 | ✓ | 제목·주소·기간·버튼·계측결과·예측결과·운영상태(휠80%·27/32) 일치 |
| status 컬러 | ✓ | 안정=green, 주의=orange, 뱃지 5색 토큰 정확 |
| 하단 프로젝트 레일 | ✓ | 카드 8개·뱃지·선택(emerald 테두리) 일치 |
| 지도 | ✓ | 도로맵(image280) 정적 배치 |
| 지도 마커 | ⚠ | 위치 근사. 원본=핀+클러스터 혼합, 재생성=리플 원형 위주 |
| 뷰토글(위성/하이브리드/도로) | ✓ | |

## 발견 구멍 → 조치
1. **로고 에셋 미추출** — GNB가 CodeConnect라 로고 asset URL 미노출. → Pass B에서 GNB 하위 노드 파고 실제 SVG 추출. (현재 CSS 다이아몬드 근사)
2. **"1차 기준 대비 (변화량)" 2줄 줄바꿈** — stat 라벨 폭. → Caption 폭/자간 미세조정 or 라벨 단축.
3. **마커 종류** — 원본 핀(spot-warning)과 클러스터 구분 필요. → pin-red/blue + cluster 구분 배치.
4. **spacing 토큰** — 패널 padding 50px 등 실측값 확보됨 → tokens에 inset 스케일 역반영 검토.

## 판정
Foundation 토큰만으로 실제 화면을 원본 수준으로 재생성 가능함을 입증(Gate 0 방향 청신호). 위 4개는 Pass B 정밀화 항목.
